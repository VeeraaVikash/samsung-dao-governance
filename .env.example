# Database (use localhost for local dev, "postgres" for Docker)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/samsung_dao"

# Auth
NEXTAUTH_SECRET="samsung-prism-dao-secret-key-min-32-chars"

# Hedera Testnet (optional — works without for MVP)
NEXT_PUBLIC_HEDERA_NETWORK="testnet"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
HEDERA_OPERATOR_ID=""
HEDERA_OPERATOR_KEY=""
